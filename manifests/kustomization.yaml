apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: openclaw
resources:
- namespace.yaml
- deployment.yaml
- service.yaml
- route.yaml
- agentruntime.yaml
- agentcard-configmap.yaml
- otel-tracing-configmap.yaml
- guardrails-config.yaml
- guardrails-adapter.yaml
# guardrails-deployment.yaml — standalone mode (use if Anthropic engine needed, CRD can't pip install)
# For product stack: use NemoGuardrails CR instead (step 14 in deploy skill)
